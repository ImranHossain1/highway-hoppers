export enum tagTypes {
  user = "user",
  schedule = "schedule",
  profile = "profile",
  bus = "bus",
  booking = "booking",
  driver = "driver",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.schedule,
  tagTypes.bus,
  tagTypes.profile,
  tagTypes.booking,
  tagTypes.driver,
];
